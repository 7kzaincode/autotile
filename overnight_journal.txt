Overnight journal for the LEGO pet generator.

Append entries below in plain English as work progresses.

00:51 - Added an anatomy-aware light coat pass inside pet_color_map.py. It derives coarse side-profile zones from the foreground silhouette: head, muzzle, chest, belly, front/rear legs, and paws. It still requires real light pixels in the photo, but now expands white/cream coat inside those zones instead of relying only on raw blur/downsampled regions. Added a pet anatomy debug PNG artifact so Zain can inspect what the system thinks is muzzle/chest/belly/paws. Added a regression test for a side pet with white muzzle, chest, underbelly, front paw, and rear paw. Targeted pipeline tests passed: 47 passed. Next I need to run broader tests and rebuild/inspect the cat output to see whether this improves the actual example or just the synthetic case.

01:10 - Wired in front-photo light profiling. The front portrait is not used for side markings, but it now informs whether muzzle/chest/front-leg/paw light coat should be trusted when the side photo has weak evidence. Rebuilt the cached cat from the existing mesh with side-profile pose metadata. The color map now logs explicit anatomy light cells: belly, chest, front leg, front paw, muzzle, rear leg, rear paw. The cached output is output/debug_cached_cat_anatomy_v3_frontlight.json with 2295 bricks and color counts roughly Tan 1279, White 920, Dark Bluish Gray 96. This is better for chest/paws/belly coverage, but I still need to validate the full app/server path and be careful that the side-map does not overpaint white on animals where only the chest is white.

01:15 - Added a regression test that side-body pet meshes skip unsafe face overlays entirely. This preserves the current safe fallback: no flat/double face, no legacy random dots on the side mesh. The face is still unsolved, but it is now intentionally blocked unless a reliable front-facing surface exists. Tests passed again: 54 passed, 2 FastAPI deprecation warnings.

01:25 - Ran the full test suite after the server path checks: 62 passed, 2 FastAPI deprecation warnings. Restarted the local viewer server on port 8766 with the current code and verified /viewer/ returns HTTP 200. The implementation now has a stronger color/anatomy pass, debug artifacts for color map / marking mask / anatomy zones, no wedge/slopes by default for pets, and safe face skipping for side-body meshes. Remaining concern: true front-facing facial detail still needs a larger geometry strategy; the current state avoids bad fake faces rather than solving the face.
